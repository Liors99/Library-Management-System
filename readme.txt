* Must have mySQL Workbench *
* In our dataparser class, we have specific information that is required to use the database.

1) Open up MySQL Workbench, and create a new connection
2) Connection name is library
3) Username is root
4) Password should be Library1
5) Leave everything else as default
6) Connect to the created instance, and go into the server tab
	-> Data import
	-> Import from self-contained file
	-> Select the given sqlf ile
	-> Start import

